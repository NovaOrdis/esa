
Fixed a defect that prevented map property components to be displayed correctly when using an output format.

line-number can be displayed as part of the output.

A few more User-Agent patterns and heuristics when the User-Agent headers are not enclosed within quotation marks.

A few more Cookie patterns and heuristics when the cookie headers are not enclosed within quotation marks.

CSV parser handles quoted fields that can contain commas

introduced UrlParser, which is used to indentify fields such as %{Referer}i